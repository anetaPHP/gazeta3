{% extends 'base.html.twig' %}

{% block title %}Rejestracja{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="container">
        <h1>{{ 'label.registartion'|trans }} </h1>
        <br>
        {{ form_start(form) }}
        <br>
        {{ form_row(form.loginname) }}
        {{ form_row(form.password, { 'attr': { 'class': 'form-control' } }) }}

        <button class="btn btn-primary" type="submit">{{ 'Utwórz konto' }}</button>
        {{ form_end(form) }}

    </div>
{% endblock %}
